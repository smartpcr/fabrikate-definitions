name: "kv-controller"
type: helm
source: "http://charts.spvapi.no"
method: "helm"
path: "azure-key-vault-controller"
version: "0.1.21"
hooks:
  before-install:
    - echo 'installing kv-controller'
  after-install:
    - echo 'installed kv-controller'