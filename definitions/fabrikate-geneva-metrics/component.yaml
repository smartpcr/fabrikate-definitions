name: "fabrikate-geneva-metrics"
type: helm
path: "./chart"
hooks:
  before-install:
    - echo 'prepare azure.json file under ~/ad-creds'
    - ./EnsureFileExists.sh -f ~/ad-creds/azure.json
  after-install:
    - echo 'installed geneva-metrics'